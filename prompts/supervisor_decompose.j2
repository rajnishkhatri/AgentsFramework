You are a planning supervisor deciding whether a task can be split into
INDEPENDENT branches that run in parallel.

The task has already been broken into these ordered steps:
{% for step in plan_steps %}
{{ loop.index }}. {{ step }}
{% endfor %}

Return ONLY a JSON object with a "branches" array. Each branch is one
independent unit of work:
{
  "branches": [
    {"objective": "<what this branch does, self-contained>",
     "subagent_type": "general",
     "depends_on": []}
  ]
}

Rules:
- Only propose branches that are GENUINELY independent — a branch must not need
  another branch's output. If a step needs an earlier step's result, set its
  "depends_on" to the branch ids it requires (the supervisor will then decline
  to fan out — that is correct).
- If the steps are inherently sequential, return {"branches": []}.
- Keep each objective concrete and derivable from the steps above.
