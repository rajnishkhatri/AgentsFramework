"""Pyramid components -- framework-agnostic domain logic.

Imports allowed: outer ``trust/``, outer ``services/``, sibling
``StructuredReasoning/trust/`` and ``StructuredReasoning/services/``,
plus stdlib + ``pydantic``. No ``langgraph`` / ``langchain``.
"""
