{
  "version": 1,
  "_comment_wi7": "Reviewer dispatch is via .cursor/rules/*.mdc path pointers -> the `code-review` skill / `python -m meta.code_reviewer --from-git-diff --prompt-version v3` CLI (WI-7). No per-edit reviewer hook is added here on purpose: even a certified LLM judge (WI-8: TPR/TNR = 1.0) should not gate every keystroke — cost + latency — so dispatch stays on-demand (deterministic-first). The afterFileEdit formatter + beforeShellExecution safety guard below are the lower rungs of the verification ladder and stay as-is.",
  "hooks": {
    "afterFileEdit": [
      {
        "command": ".venv/bin/python scripts/hooks/cursor_after_edit.py",
        "_comment_failClosed": "Intentionally false (advisory formatter, HOOK-1 never-block-on-edit): a formatter hiccup must NOT block an edit. Scoped deviation from the harness plan's blanket failClosed:true; the safety gate beforeShellExecution stays true. See docs/adr/decisions.md (2026-06-28).",
        "failClosed": false
      }
    ],
    "beforeShellExecution": [
      {
        "command": ".venv/bin/python scripts/hooks/cursor_before_shell.py",
        "failClosed": true
      }
    ]
  }
}
