{
  "font": {
    "sans": {
      "$value": "var(--font-geist-sans), \"Inter\", ui-sans-serif, -apple-system, BlinkMacSystemFont, \"Segoe UI\", Roboto, \"Helvetica Neue\", Arial, sans-serif",
      "$type": "fontFamily",
      "$description": "var(--font-geist-sans) is a runtime CSS var set on <html> by Next.js app/layout.tsx via the geist package — must be preserved verbatim"
    },
    "mono": {
      "$value": "var(--font-geist-mono), \"JetBrains Mono\", ui-monospace, SFMono-Regular, Menlo, Monaco, monospace",
      "$type": "fontFamily",
      "$description": "var(--font-geist-mono) is a runtime CSS var set on <html> by Next.js app/layout.tsx via the geist package — must be preserved verbatim"
    }
  }
}
