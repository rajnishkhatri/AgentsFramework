## Summary

- Adds `Dockerfile.searxng` with Cloud Run-specific settings (`searxng/settings.cloudrun.yml`): listens on port 8888, JSON output enabled, rate limiter off (no Redis).
- Fixes the Cloud Run sidecar image reference to use the **backend** Artifact Registry repo (replacing a broken `docker` repo reference that does not exist in Terraform).
- Updates `deploy_gcp.sh` to **build and push** the custom sidecar image instead of mirroring upstream verbatim, so production gets the baked-in settings.
- Tunes the sidecar startup probe (`initial_delay_seconds = 10`) and sets `SEARXNG_BASE_URL` / `GRANIAN_PORT` env vars for the sidecar container.

## Test plan

- [ ] `docker build -f Dockerfile.searxng -t searxng:local .` succeeds
- [ ] `docker run --rm -p 8888:8888 searxng:local` — `curl localhost:8888/healthz` returns OK
- [ ] `curl 'localhost:8888/search?q=test&format=json'` returns JSON results
- [ ] `tofu validate` / `conftest test` in `infra/gcp/` (sidecar image ref resolves to `backend` repo)
- [ ] Deploy dry-run: `DRY_RUN=1 ./scripts/deploy_gcp.sh push` shows Dockerfile.searxng build step
