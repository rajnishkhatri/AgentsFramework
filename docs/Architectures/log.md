---
type: log
title: 'Architecture documents — bundle log'
---

# Architecture documents — bundle log

Chronological history, newest first (ISO-8601).

- 2026-07-02 — **Pre-implementation gap review closed (10 gaps) across both Subject-Coach design docs.** [SUBJECT_COACH_AGENT_DETAILED_DESIGN.md](SUBJECT_COACH_AGENT_DETAILED_DESIGN.md): §4.1 rewritten to the **two-layer context assembly** (ADR-0012 Amendment — minimal BFF coach-session marker store for mode derivation + BFF-side answer-field strip over client-supplied content; `misses_aggregate` structured, never raw prose into the prompt); §4.2 interim authored rungs → backend-readable data asset; §2 catalog completed (judge-injection row corrected to NOT-NEEDED — `GoalJudge` internally constructed, coach judges off-graph; marker store/sanitizer/rung-asset/§12/§13 artifact rows added; posture flag BUILT); §3 rate-limit row → **no middleware rate seam exists** (verified; new-infra Ask-first item); §6 held-out guardrail fixture designed; §7.1 per-criterion binary calibration fields; §9 unverified edge (1) resolved-absent; §10 ADR-0012 amendment note + ADR-0008 cond#2 restated (judges+reader+sampler in one increment); §11 steps 2/3 re-scoped; §12.1 shadow-traffic provenance statement. [SUBJECT_COACH_DETAILED_COMPONENT_DESIGN.md](SUBJECT_COACH_DETAILED_COMPONENT_DESIGN.md): §2.3 stale rows flipped — Dashboard/Coach/Summary/routes/**coach BFF route** all BUILT (verified against `frontend/components/*` + `app/api/coach/run/stream/route.ts`); marker-store row added. Docs-only; pairs with the ADR-0012 amendment log line in `docs/adr/log.md`.
- 2026-07-02 — **Test-Mode design sections landed (the ADR-0013 execution pass, post-ratification).** [SUBJECT_COACH_DETAILED_COMPONENT_DESIGN.md](SUBJECT_COACH_DETAILED_COMPONENT_DESIGN.md): NEW §2.5 (Test Mode surface catalog — BUILT rows with file:line incl. the ungoverned `convert:test01`; TO-BUILD `TestBlueprint` + assembler; DEFERRED archival = a stake-trigger event) + NEW §4.9 (per-screen (a)–(f): React-free reducer core, `Grader`-only port surface, the integrity finding resolved by ADR-0013), §2.3 Screen-8 row, §7 pointer, §8 step 6. [SUBJECT_COACH_AGENT_DETAILED_DESIGN.md](SUBJECT_COACH_AGENT_DETAILED_DESIGN.md): NEW §8.1 (content families — the per-stage cascade mapping table; answer-key consistency = the test family's critical gate, leakage rung N/A) + §8.2 (blueprint + seeded deterministic assembler; client-side under Option A) + §8.3 (`convert:test01` → seed importer; rows enter `reviewed=false`), §9 Ask-first row extended, §10 ADR-0013 adjudication (ACCEPTED W/ CONDITIONS — the three tripwires + the pending FR-28 posture-flag condition), §11 step 7. Docs-only.
- 2026-07-02 — [SUBJECT_COACH_AGENT_DETAILED_DESIGN.md](SUBJECT_COACH_AGENT_DETAILED_DESIGN.md) gained **§13 governance trace-audit plane**, binding the coach to the [governance-trace-audit](../skills/governance-trace-audit/SKILL.md) four-pillar contract: per-pillar carrier map with coach expectations (tool vocabulary = think/file_io only; `subject-coach-english` identity values), the **coach-shape rubric amendment** (`eval.goal_judge` absent inline is EXPECTED per ADR-0009 — evidence lives in the post-hoc `coach_judges` stream), and the coach's corrupt-success analog: the **§13.2 context-contract compliance check** (a pre-submit trace carrying any answer-bearing field = NON-COMPLIANT seam defect). §9 gained the every-policy-has-a-carrier invariant; §11 steps 2/3 wire the audit in as the §12.1 garbage-in guard + red-first coach fixtures. Docs-only.
- 2026-07-02 — Expanded [SUBJECT_COACH_AGENT_DETAILED_DESIGN.md](SUBJECT_COACH_AGENT_DETAILED_DESIGN.md): §7 gained §12 pointers and the binding-floor cross-ref; NEW §12 instantiates the [llm-eval-grounded-theory](../skills/llm-eval-grounded-theory/SKILL.md) 8-stage pipeline for the coach's two judges (one shared trace pool + taxonomy + `coach_goldset_v1`, two rubrics calibrated separately; `answer_leakage` the sole action-trigger class; the PROVISIONAL-rubric posture resolves the traces-before-rubric ordering). §9 gained the synthetic-contamination-firewall + never-tune-on-test invariants; §10 records the ADR-0008 cond#1 progression (augmented, not amended); §11 steps 2/3/4/6 cross-wired to §12 stages. Docs-only.
- 2026-06-20 — Declared `docs/Architectures/` an OKF bundle: added `index.md` + this `log.md` and typed frontmatter on every Concept (pure prepend, bodies unchanged). Convention in [CONVENTIONS_OKF.md](../CONVENTIONS_OKF.md); linted by `scripts/okf_lint.py`.
