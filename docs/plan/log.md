---
type: log
title: 'Roadmap & layered plans — bundle log'
---

# Roadmap & layered plans — bundle log

Chronological history, newest first (ISO-8601).

- 2026-07-17 — **Eng-coach Gen2 / v2 adoption — baseline validated + architecture models drafted.** Every session-doc §2 claim re-derived green (deterministic `jq` + repo sweep); four adoption-seam artifacts (use case / domain / component / Phase-1 LLD) drafted and adversarially verified (8 corrections, 2 major — nonexistent peer-import edge; `seedHints` silent-overwrite second write path). New findings: coach wire has NO letter field (moment router = new wire field end-to-end); 388/1000 Gen2 items on standards 33–43 with `standard_id` stripped at wire pending D4; free-ask × `BANK_RUNGS` duplicate-rung gap → proposed lock **P0.8**. 30-item Gen2 probe: 0 key defects, patterns P1–P3; full grounded-theory eval not warranted — rubric→judge tail on 2 lenses + 4 deterministic lints. Doc: [eng-coach-gen2-v2-adoption.architecture.md](eng-coach-gen2-v2-adoption.architecture.md) (auditable v1); readable rewrite with inline Mermaid = [architecture.v2.md](eng-coach-gen2-v2-adoption.architecture.v2.md). Next: human lock P0.1–P0.8 → `sdd-spec` Phase 1.

- 2026-07-17 — **Eng-coach Gen2 / v2 adoption — exploratory session captured.** Devil's-advocate on Gen2 bank/hints, v2 Axis A/B gap audit, Gen1↔Gen2 same-item A/B (incl. inverse), hybrid critique (reject layout- and skill-swap hybrids; adopt moment-based pedagogy). Recommended Path A + phased todo P0–P4. Session: [eng-coach-gen2-v2-adoption.session.md](eng-coach-gen2-v2-adoption.session.md). Next: human lock Path A/B/C → Phase 0 → `sdd-spec`.

- 2026-07-16 — **Wide-layout & CoachPanel parity — Stage 4 analyze PASSED.** Cross-artifact: FR-1…20 all covered; no CRITICAL path/invariant gaps; no new deps; ADR-0035 amended. Grounding: all existing touchpoints present; NEW (CoachDrawer/pill/expandable/HintLadder) correctly absent; DELETE (`use_collapsible_thread`) present. Draft seams (Sheet/edge-tab/pin/56px) still in tree as expected. Non-CRITICAL: rename `SIDEBAR_COLLAPSED_PX`→`RAIL_COLLAPSED=64` in T1.1. Baseline: `make check` 5320 passed; scoped vitest shell+coach+Composer 129 passed. **Next:** human Accept → `sdd-implement` L-track. Tasks: [preact-wide-layout-coach-panel.tasks.md](preact-wide-layout-coach-panel.tasks.md) §Stage 4.

- 2026-07-16 — **Wide-layout & CoachPanel parity — Stage 3 tasks Accepted (L0–L6).** Replaced invalidated W0–W9 board with Direction 2b L-track: measurability checklist FR-1…20, FR→task coverage table, atomic T0.1–T6.1 with `[red]`/`[P]`/Pass·Fail. Parallel L1‖L2 after L0; L4 after L1+L3; L5 e2e; L6 Safari/`dvh`. Tasks: [preact-wide-layout-coach-panel.tasks.md](preact-wide-layout-coach-panel.tasks.md).

- 2026-07-16 — **Wide-layout & CoachPanel parity — Stage 2 re-Accepted for Direction 2b lock.** Stage 5 replan: locked-spec-artifacts supersede draft FR-1…16 / 56px / pin / Sheet / `use_collapsible_thread`. Clarify Q-C1…Q-C5 CLOSED (nudge Zone C; amend ADR-0035; layout-local content rail; CoachDrawer not Sheet chrome; `use_expandable_list`). Spec FR-1…20 ≡ locked AC-1…20; plan L0–L6 foreshadow. Spec/plan: [preact-wide-layout-coach-panel.spec.md](preact-wide-layout-coach-panel.spec.md) · [plan](preact-wide-layout-coach-panel.plan.md). Lock: [`locked-spec-artifacts/`](../width-design-ui-session-artifacts/locked-spec-artifacts/).

- 2026-07-16 — **CoachPanel real-estate + Zone-B scroll — Stage-2 draft spec authored.** W9 iPad screenshots showed cramped coach (`w-80` / 320px), chips+modes clipped, and Nudge 2 cut off by Zone C. Root cause grounded: revealed nudges sit as `shrink-0` siblings *outside* `coach-log` while Zone B is `overflow-hidden`. Draft EARS FR-1..7 in [preact-coach-panel-real-estate.spec.md](preact-coach-panel-real-estate.spec.md); clarify C1–C4 OPEN (width target, single scrollport, long-answer policy, scope vs parent W9). Plan/tasks deferred until Accept. No product code this stage.

- 2026-07-16 — **W9 height-chain preflight + layout fix (device sign-off still open).** FR-9 e2e added in `wide-layout.spec.ts` (window locked; coach-log scrolls; Home/Progress `main` page-scroll). Probe found Zone A/C starving Zone B (~13px log) on iPad width — removed duplicate Socratic header; stacked modes + chips use horizontal scroll instead of wrap; Zone B/CoachView overflow chain tightened. Playwright preflight **6/6 green**. Manual Safari/`dvh` checklist lives under W9 in [tasks](preact-wide-layout-coach-panel.tasks.md) — needs human iPad pass.

- 2026-07-16 — **Wide-layout & CoachPanel parity — Stage 6 implementation landed (W1–W8).** Spec Accepted; ADR-0035 Accepted. Unit evidence: shell + collapsible + CoachView/Panel/Workspace vitest green (69 tests in scoped run). Product: `isWideSurface`/`SPLIT_MIN_CONTENT_WIDTH` ladder; `shell_layout_store`; icon-rail + `[` + ThemeToggle; CoachPanel Zones A/B/C + Sheet drawer + edge-tab dismiss; E1 pin+focus Feedback bridge on wide; e2e `wide-layout.spec.ts`. **W9** Safari/`dvh` height-chain remains a manual DoD spike on real iPad. Spec: [preact-wide-layout-coach-panel.spec.md](preact-wide-layout-coach-panel.spec.md).

- 2026-07-16 — **Wide-layout & CoachPanel parity — SDD Stages 2–4 authored (Draft).** Converted design-agent Decision Record + Implementation Spec into repo-native OKF: [spec](preact-wide-layout-coach-panel.spec.md) (EARS FR-1..16; AC-15 split into FR-15 persistence + FR-16 reduced-motion), [plan](preact-wide-layout-coach-panel.plan.md) (`isWideSurface` / `shell_layout_store` / height chain / Sheet reuse / Zones A–C), [tasks](preact-wide-layout-coach-panel.tasks.md) (W0–W9 red-first, parallel W1‖W2‖W4). Locked product: B1 icon rail on content-heavy only; E1 in-place Feedback bridge; A2+A3 ladder + dismiss; mid-width iPad may use drawer. Raises [ADR-0035](../adr/0035-wide-layout-coach-panel-parity.md) (G1). Stage-4 grounding: all cited seams exist (`use_surface`, `layout`, `AppNav`, `quiz/page` still `surface==="ipad"`, `CoachPanel/View/Workspace`, `coach_thread_store`, `ui/sheet`). Design folder cited as Related only. **Human gate:** Accept spec → then `sdd-implement` (no product code in this stage).

- 2026-07-15 — **Eng-coach Tauri Mac shell — SDD Stage-1 brainstorm OPEN.** Premise audit for "create Tauri Mac desktop app for GCP eng coach" **refuted greenfield scaffold**: Tauri 2 + `agentsframework://` WorkOS PKCE already at [`frontend/src-tauri/`](../../frontend/src-tauri/) (ADR-0001, P5); release `PROD_URL` already points at Cloud Run `agent-frontend`. Re-posed as product specialization (coach-first vs dual-product) + packaging bar, with D0s: empty/unseeded live `/learn` (sibling D7), deferred live Tauri auth smoke, `PROD_URL` drift. Directions D1–D7 (incl. demand-side defer). Gate Q1–Q4. Brainstorm: [eng-coach-tauri-mac-shell.brainstorm.md](eng-coach-tauri-mac-shell.brainstorm.md).

- 2026-07-15 — **Eng-coach GCP deploy — Stage-3 tasks authored (plan→tasks gate PASSED).** Decomposed T1–T10 into red-first Groups **A–F** with a 1:1 FR→task→test table (no zero-coverage FR; FR-2 covered by the existing `(coach)/layout` guard). **Resolved the plan's flagged design risk:** the FR-1..5 prod-branch tests can't rely on flipping `NODE_ENV` (Next/bundler inlines `process.env.NODE_ENV`; no `vi.stubEnv` prod hook exists in the seedmode suite) → extract a **pure `planEngineSeed({isProd,seedMode})` helper** (`(true,"fresh")→fresh-pack`; `(true,"demo"|unset)→none`) that `browserEngineAdapters()` delegates to after computing `isProd` once; the helper is L1-unit-testable with no env flip and is a pure extraction (no new abstraction/ADR). Groups A–D parallel (disjoint files); E depends on A (proof needs the seed); F (measure `ANALYZE` + deploy) last. Tasks: [eng-coach-gcp-deploy.tasks.md](eng-coach-gcp-deploy.tasks.md). **tasks→implement gate pending Stage-4 analyze + baseline green.**

- 2026-07-15 — **Eng-coach GCP deploy — Stage-2 spec→plan gate PASSED; plan + 2 ADRs authored.** Human sign-off with two tightenings, both **validated against the tree before accepting**: **FR-1** widened from "SHALL NOT seed `seedDevCorpus`" (too narrow — permits a buggy prod-`demo` path to still call `seedDevTaxonomy`/bank) to **prod ∧ `seedMode≠"fresh"` → empty bag, seed nothing** (FR-3 the only positive prod seed path; AP-6). **FR-15** rewritten after the feedback caught a real defect: my "URL present + still `InMemoryCoachMarkerRepo`" tombstone tests an **unreachable** state — `selectCoachMarkerRepo` ([marker_repo.ts:111-119](../../frontend/lib/adapters/coach_marker/marker_repo.ts)) switches to Pg the instant `DATABASE_URL` is set; the real forgot-D4 hole is **TF binds `DATABASE_URL` without a `0001` migrate path** → `PgCoachMarkerRepo` on an un-migrated table strips answers forever via its fail-closed `catch` (:101-105). Rewrote to the **ADR-0013 tripwire shape** (green today / red on conscious future change — NOT red-in-CI-today; confirmed the precedent `test_no_client_served_test_keys.py` is exactly that shape). **Plan** [eng-coach-gcp-deploy.plan.md](eng-coach-gcp-deploy.plan.md): T1–T10 file-level touchpoints, Frontend-ring + `scripts/` only, no infra/secret/backend change; the bundle static-vs-lazy call is a **measure-first DoD gate** (`ANALYZE=true` vs `.bundle-baseline.json`). **ADRs** [0033](../adr/0033-coach-prod-web-seed-fresh-mode.md) (D7 seed-gate lift + web-vs-native split) + [0034](../adr/0034-coach-marker-in-memory-until-threads-bind.md) (Q3 in-memory time-box, D4 the closer). One design risk flagged for tasks (prod-branch tests need a `NODE_ENV=production` hook or a small injectable `isProd` seam in T1). **Plan→tasks gate pending.**

- 2026-07-15 — **Eng-coach GCP deploy — SDD Stage-2 spec authored (`eng-coach-gcp-deploy.spec.md`, Draft).** Ran sdd-spec Specify + Clarify from the closed brainstorm. **Clarify (3 decisions, verified against the tree first):** (1) D7 makes ~12K lines of corpus (`_test_item_bank.ts` 7.5K + `_hint_bank.ts` 4.6K, today dead-code-eliminated by the `NODE_ENV` guard at [composition_engine_browser.ts:216](../../frontend/lib/composition_engine_browser.ts)) prod-reachable → bundle static-vs-lazy is a **plan decision after measuring `ANALYZE=true`**, spec states only the requirement; (2) Q5 proof = automated curl `/`+`/learn` 307-to-WorkOS + a **manual** authed render eyeball (no test creds in the pipeline; `/learn` is a WorkOS-session RSC page, not a bearer-token API); (3) Q3 accepted cost guarded by an **architecture-test tombstone** (fails when `DATABASE_URL` reaches the frontend while the marker repo is still in-memory — enforces the D4 time-box). **Grounded seams** (explore): no `/learn` link exists anywhere (D2 insertion = [chat-shell.tsx:538-560](../../frontend/app/chat-shell.tsx) action cluster); sole sign-in CTA = [page.tsx:64](../../frontend/app/page.tsx) (iOS selector `composition_ios.ts:26` is substring-match, `?returnTo` safe); smoke curls `/` only ([smoke_gcp.sh:67](../../scripts/smoke_gcp.sh)); frontend has **no migration runner** ([drizzle.config.ts:23-25](../../frontend/drizzle.config.ts) documents its own gap) — confirms D3/Q3. Spec = **FR-1..15, failure-first**; seed fns carry no internal `NODE_ENV` guard (prod-safe to invoke — the guard is caller-side only). Raises 2 ADRs (D7 seed-gate lift; Q3 time-box). **Spec→plan human gate pending.** Spec: [eng-coach-gcp-deploy.spec.md](eng-coach-gcp-deploy.spec.md).

- 2026-07-15 — **Eng-coach GCP deploy — Stage-1 gate RE-CLOSED (all 5 Qs answered).** After the reopen (below), a second critical review of the resolution was verified against the tree before acceptance, and every load-bearing claim checked out. **Q2 = D7** — seed a reviewed prod web corpus in `fresh` mode (un-gate the prod empty-substrate branch; `seedMode=fresh` = taxonomy+bank+hints+lessons, **no Garvit mastery** — that's the separate `seedDevCorpus`); verified [`_dev_seed.ts:264-269`](../../frontend/lib/adapters/engine/_dev_seed.ts). **Q1 = agent-peer, web CTA-only `getSignInUrl({returnTo:"/learn"})`** — D1 global flip rejected (`getSignInUrl()`+`handleAuth()` with no `returnPathname` is a *shared* agent+eval+coach default, [route.ts:24,46](../../frontend/app/api/auth/%5B...workos%5D/route.ts)); D6 policy deferred (`resolveLearnIdentity` has **zero** role/org branching — every WorkOS user → `learnerId=user.id`/`fresh`, [resolve_learn_identity.ts:43-64](../../frontend/lib/learn/resolve_learn_identity.ts) — nothing to route on); **caveat:** web-only, `DESKTOP_POST_AUTH_PATH="/"` ([desktop_auth_state.ts:88](../../frontend/lib/adapters/auth/desktop_auth_state.ts)) is a separate Tauri constant reached before `getSignInUrl`, desktop `/` unchanged. **Q3 = defer** (in-memory marker, time-boxed to the threads BFF Cloud SQL bind, then close via D4 on the *same* `DATABASE_URL` switch) — D5 rejected: it amends ADR-0012 (whose 2026-07-02 Amendment **is** the marker-on-BFF decision, confirmed) and stands up a 2nd durability plane while `bff_cloudsql_thread_repo.plan.md` already routes threads to the BFF; **consistent with [decisions.md:594-595](../adr/decisions.md)** "Rejected: durable store" already ratified on this branch; accepted cost = known multi-instance/cold-start marker flapping (fails *closed* — quality gap, not a leak). **Q4 = D2-in-v1** (one `/learn` link). **Q5 = authed `/learn` render smoke** (proves D7 end-to-end; `/`-200 rejected; coach-SSE-with-JWT = optional stretch since the SSE 401s without a verified JWT, out of proportion for a first deploy). **Deliverable = D7 + web CTA `returnTo:/learn` + D2 + D3 + `/learn` render smoke; Q3 defer; D1/D5/D6 out.** Two ADR triggers for the spec: D7 seed-gate lift (+web-vs-native split) and the Q3 in-memory-until-threads-bind time-box. Brainstorm: [eng-coach-gcp-deploy.brainstorm.md](eng-coach-gcp-deploy.brainstorm.md). **ADVANCE → sdd-spec.**

- 2026-07-15 — **Eng-coach GCP deploy — Stage-1 gate REOPENED after devil's-advocate review ("good brainstorm, bad close").** The same-day close (D1+D5+D3) was withdrawn once a critical review was verified against the tree. **Blocking miss D0-b:** prod `/learn` renders on an EMPTY substrate — `composition_engine_browser.ts:257-261` builds an empty `InMemoryEngineDb` under `NODE_ENV=production` with every seed gated to non-prod, and the intended on-device SQLite data path (ADR-0005/0010) is unbuilt **and Capacitor-only**, not the Cloud Run web frontend. So D1+D3 land every user on a working-auth / empty-product `/learn`; the first pass's only `needs-probe` ("is the image digest fresh?") was the wrong probe. Review holes confirmed: D5's "F-R9-clean" is oversold (real arg = blast radius; frontend already holds `WORKOS_API_KEY`) and it **amends ADR-0012** (marker-on-BFF); D4 is less novel than claimed — `docs/plans/bff_cloudsql_thread_repo.plan.md` already plans BFF Cloud SQL on the same `DATABASE_URL` switch, so D5-only risks two durability planes; the learner-identity slice **rejected a durable store** ([decisions.md](../adr/decisions.md)) unreconciled with Q3; and "everyone → /learn" silently decided a **dual-product** front-door question. Doc corrected (D0-b added; D1/D3/D4/D5 claims fixed); gate now holds on **5 product questions** (front door / data plane / durability plane / reachability / deploy proof). NEXT = answer the 5 Qs, then sdd-spec (must at minimum resolve the empty-substrate question). Brainstorm: [eng-coach-gcp-deploy.brainstorm.md](eng-coach-gcp-deploy.brainstorm.md).

- 2026-07-15 — **Eng-coach GCP deploy — SDD Stage-1 brainstorm CLOSED.** Ran the premise audit for "deploy the Eng Coach to GCP dev + fix the WorkOS sign-in surface + land on the coach dashboard post-login" and **refuted 3 of 4 premises** against the working tree: the coach is *not* a first-time deploy — it already ships as route-group pages inside the deployed `agent-frontend` Cloud Run service ([`infra/gcp/cloud-run-frontend.tf:30`](../../infra/gcp/cloud-run-frontend.tf)); the coach "dashboard" is the `/learn` route, not a `/dashboard` page ([`frontend/app/(coach)/learn/page.tsx`](../../frontend/app/%28coach%29/learn/page.tsx)); and the coach has no missing sign-in button — its layout forces `withAuth({ ensureSignedIn: true })` before paint ([`frontend/app/(coach)/layout.tsx:31`](../../frontend/app/%28coach%29/layout.tsx)). The one **confirmed** gap is the post-login redirect: `handleAuth()` carries no `returnPathname` so AuthKit lands `/` ([`frontend/app/api/auth/[...workos]/route.ts:24`](../../frontend/app/api/auth/%5B...workos%5D/route.ts)). Audit also surfaced a live **D0 risk** — the durable submit-once marker is dead in prod because the frontend service holds no `DATABASE_URL` (falls back to `InMemoryCoachMarkerRepo`; fails CLOSED, so no leak, but non-durable). **Gate CLOSED (2026-07-15):** Q1 framing accepted (proceed on `/learn`); Q2 = **D1** land everyone on `/learn` via `handleAuth({ returnPathname: "/learn" })`; Q3 = **D5** route the marker through the middleware (F-R9-clean; new backend endpoint, ADR at spec). Net deliverable = **D1 + D5 + D3** (fresh `agent-frontend` redeploy, gated on a live-revision-freshness probe). Brainstorm: [eng-coach-gcp-deploy.brainstorm.md](eng-coach-gcp-deploy.brainstorm.md). NEXT → sdd-spec.

- 2026-07-13 — **Epic-F parity-review fixes (honest-null D-B class).** Cleared the parity-review findings on the `/learn` surface with one convention, not scattered patches: the translator/VM seam now emits an explicit "unknown" signal (`null` / `"—"` / a `*Known` flag) whenever the underlying datum is absent, never a plausible zero the view renders as real data. Applied — `bucket_card_vm.masteryKnown` (no `role="progressbar"` / `aria-valuenow` when mastery is unknown; a genuine measured 0% still renders), `session_summary_vm.timeTile` `"<1 min"` for sub-minute sessions (was "0 min"), `use_coach_surface.skillNameById` returns `null` on an unresolved skill (never the raw `s-*` id). Each fix carries a per-VM guard test (`bucket_card_vm.test.ts::bucket_missing_mastery_is_honest_not_zero`, etc.) + view-level "no data, not a 0% bar" guards in `BucketCard`/`ProgressView`; SD-6 bucket→`/learn/skill` navigation gets an e2e integration guard (`e2e/learn/skill-detail.spec.ts`). Spec refined at [preact-parity-epic-F.spec.md](preact-parity-epic-F.spec.md) FR-4; the convention is captured in [decisions.md](../adr/decisions.md) (no ADR — convention, not a new abstraction). Evidence: 83/83 changed-file vitest green; e2e walk report + screenshots under [assets/preact-parity-e2e-2026-07-13/](assets/preact-parity-e2e-2026-07-13/) and [validation report](preact-parity-e2e-validation-report-2026-07-13.md). Handoff: [preact-parity-epic-F.HANDOFF.md](preact-parity-epic-F.HANDOFF.md).

- 2026-07-03 — **Task 3.3a–3.3d tooling BUILT** (sdd-implement, red/green TDD) on [coach-goldset-enable-policy](coach-goldset-enable-policy.tasks.md): four ‖py scripts + tests, each written failure-first (watched red before green). **3.3a** `scripts/build_coach_open_coding_cases.py` — thin adapter reusing `export_coach_coding_sample.{check_posture,build_coder_rows}` with `holdout_fraction=0.0` (full eligible pool), fail-closed `SubFloorError` below 100/mode, blank-answer exclusion (FR-G3.1.3), mode-contiguous `cases.json` (9 tests). **3.3b** `scripts/verify_coded_open_codes.py` — flags empty-`open_codes` rows + per-mode count (FR-G3.1.4; 3 tests). **3.3c** `scripts/build_coach_open_code_inventory.py` — coded.jsonl → GoalJudge-column inventory CSV, one row/distinct code (FR-G3.1.9; 5 tests). **3.3d** `scripts/export_coach_open_coding_to_dataset.py` — coach wrapper over the skill exporter, pinned dataset `coach-phase3-open-coding`, `uuid5(trace_id)` idempotent id, **dry-run default** with lazy Langfuse import so the dry-run path needs no creds/network (FR-G3.1.11–.12; 4 tests). **Real-data smoke** of 3.3a over `logs/evals.log` + `merged_batch2_2b_manifest.json --manifest-only`: **237 cases (122 pre / 115 post)**, single mode transition, all required keys, zero blank answers — matches the 3.2b posture-eligible pool exactly, both modes clear ≥100. `make check` **green: 4927 passed, 51 skipped** (+21 = the four new test files); architecture suite green. 3.3a–d done; hands off to the HUMAN gate 3.3e (code ≥100/mode) → 3.3f (artifacts).

- 2026-07-03 — **Task 3.3 SDD spec+plan+tasks pass** on [coach-goldset-enable-policy](coach-goldset-enable-policy.spec.md): ran sdd-spec (Stages 2–4) for human open coding. Clarify pass ratified 3 decisions — (1) code the **merged coding-eligible pool** (~122 pre / ~115 post) not the 169-row sample (under-fills both modes vs the ≥100/mode floor), (2) attest saturation via a **per-mode saturation log** (checkable, not prose), (3) additionally upsert coded cases to Langfuse dataset `coach-phase3-open-coding`. Folded the executable expansion into **FR-G3.1.1–.12** in the parent spec (+ §8 test rows) rather than a standalone file (per user). Decomposed Task 3.3 → **3.3a–3.3f** (4 ‖py tooling tasks + 2 HUMAN gates). **Stage-4 analyze caught a real join defect:** the plan first assumed `trace_id`→answer joined from `outcomes.jsonl`, but that file has neither `trace_id` nor the coach answer — the answer lives in `logs/evals.log` EvalRecords, and `export_coach_coding_sample.build_coder_rows` **already** emits coder-ready rows (`trace_id/prompt/final_answer/mode/…`). Corrected 3.3a to a **thin adapter that reuses `build_coder_rows`** (no rebuilt join → satisfies the plan's Reuse principle + G1). No ADR trigger (build/verify scripts; coder+exporter+`langfuse_dataset_client.py` pre-exist; no new dep/service/node/abstraction). Baseline gate: `tests/architecture/` green (158 passed, 3 skipped). Ready for sdd-implement (3.3a first, failure-first).

- 2026-07-03 — **Task 3.2b live batch-2b run + empirical gate confirmation** on [coach-goldset-enable-policy](coach-goldset-enable-policy.tasks.md): executed the live harvest (`scripts/build_coach_shadow_corpus_batch2b.py --base-url http://127.0.0.1:8124`, middleware on PORT=8124 since 8123 was held by a stale instance) — **50/50 rows finished, 0 failed** in ~84 s; eval records landed in `logs/evals.log` (`target="subject_coach"`, provenance synthetic). Empirical eval-record join: all 25 unique batch-2b utterances produced a coach eval record → **100% guardrail clearance** (0 refusals), matching the reasoned projection exactly. Merged export (`export_coach_coding_sample.py --input logs/evals.log --manifest merged_batch2_2b_manifest.json --manifest-only --seed 42`) printed **122 pre / 115 post posture-eligible, gate MET** (+1/mode over the 121/114 projection — posture checker counts eval records per mode vs the projection's utterance-level count; both confirm the gate). Shipped: `cache/coach_shadow/merged_batch2_2b_manifest.json` (250 rows), `batch2b_coding_sample.jsonl` (**169 rows** = 88 pre / 81 post, all manifest-joined), `batch2b_holdout_ledger.json` (seed 42, 68 task_ids, **disjoint from coding sample, overlap=0**). Phase-3 entry gate empirically cleared; coding sample ready for Task 3.3 open coding. Updated [batch2_posture_report.md](../cache/coach_shadow/batch2_posture_report.md) with the empirical confirmation + shipped-artifact manifest.

- 2026-07-03 — **Task 3.2b reasoned gate validation** on [coach-goldset-enable-policy](coach-goldset-enable-policy.tasks.md): batch-2 fell 9 pre / 6 post short of the ≥100/mode gate (15 hard-strata rows guardrail-refused). Authored batch-2b gap-fill (`scripts/build_coach_shadow_corpus_batch2b.py` + 20 tests + `cache/coach_shadow/batch2b/manifest.json`, 50 rows, non-refused strata only, §9-fresh). Gate-clearance settled by **reasoning from batch-2 evidence** (not a live LLM run, per user instruction): joining batch-2's 200-row manifest to the 292 coach eval records in `logs/evals.log` shows all 4 non-refused strata cleared the guardrail at 100% (breadth 140/140, rule_naming 6/6, leak_bait 4/4, overgeneralization 10/10) while all 15 refusals came from the 4 refused strata batch-2b excludes. ∴ all 50 batch-2b rows produce a `target="subject_coach"` eval record → merged 121 pre / 114 post, gate MET by construction. Reasoned replies deliberately NOT written into `logs/evals.log` (would contaminate the FR-G3 coding sample with non-agent behavior); live run's remaining scope narrowed to reply-capture for Task 3.3, not gate validation. Updated [batch2_posture_report.md](../cache/coach_shadow/batch2_posture_report.md) with the evidence table. `make check` green (4906 passed); architecture tests green (158 passed).

- 2026-07-03 — **Stage-4 cross-check fixes** on [coach-goldset-enable-policy.spec.md](coach-goldset-enable-policy.spec.md) + [plan](coach-goldset-enable-policy.plan.md) + [tasks](coach-goldset-enable-policy.tasks.md): added 6 missing automated tests to §8 (FR-G4.2 orphan-criteria, FR-G4.3 flags-off-until-ENABLE, FR-G5.5 manifest-α-refuse, FR-G5.7 `mistake_location_pass` field, FR-G6.3 augmenting gates, FR-G6.7 κ<0.6 unreliable); added `tests/services/test_coach_goldset_dataset.py` to task 3.7b file list; added 3.7b → 3.2b dependency (holdout ledger seeds test-split candidacy, FR-G5.4). Every automated FR now has a named test.

- 2026-07-03 — **Critical review pass** on [coach-goldset-enable-policy.spec.md](coach-goldset-enable-policy.spec.md): fixed eval-log vs blackbox posture mismatch (C9), eval-capture 200/500 truncation reality (C12), open-coder field map (C11), holdout ledger + tests for FR-G2.1, coded-entry shortfall FR-G2.5, manifest join FR-G5.6, production-subset precision FR-G6.6. Added clarify C9–C12, task **3.6b** (taxonomy frozen set), revised [plan](coach-goldset-enable-policy.plan.md) + [tasks](coach-goldset-enable-policy.tasks.md). Spec→plan human gate pending.

- 2026-07-03 — Added [coach-goldset-enable-policy.spec.md](coach-goldset-enable-policy.spec.md) (SDD Stage-2 Phase-3 spec: EARS FR-G1..G7 for posture checker, coding-sample export, human open/axial coding gates, REVISED rubrics, `CoachGoldsetItem` + `coach_goldset_v1` assembly, `evaluate_coach_enable_gates` cert — closes ADR-0008 cond#1). Clarify pass CLOSED (8 decisions: synthetic corpus path, coded-vs-raw gate, leak-only action trigger, binding TPR/TNR/κ floor, 200–300/60-40 gold set, open-coding tooling reuse, telemetry-only until ENABLE, optional gap-fill). Derived [plan](coach-goldset-enable-policy.plan.md) + [tasks](coach-goldset-enable-policy.tasks.md). Spec→plan human gate pending.

- 2026-07-04 — **ADR-0016 drafted + task design split into its own doc.** Drafted [ADR-0016](../adr/0016-subject-coach-learning-event-append-plane.md) (PROPOSED — learner-behavior event append plane; ADR-0006 **fourth amendment, first with a write surface**; 12th port `LearningEventRepo` **applies** #3 `AttemptRepo` append+scoped-read; decides the Test-Mode isolation force explicitly + amends the `test/page.tsx` header; coach `run_ref`=real `trace_id`|NULL never the `"no-trace"` sentinel; `task_id` into `run_started` details as the D5 crosswalk source) with full OKF (index/log, ADR-0006 header + 12th port-table row). Then **extracted the FR→structure→task-group map out of spec §10 into a standalone design doc** [coach-learning-analytics.design.md](coach-learning-analytics.design.md) (spec now rev-3 with a §10 pointer only, staying the pure EARS `what`); the design is the join between spec/plan/tasks and asserts no zero-coverage FR. `test_adr_ratchet.py` passes; okf_lint 0 failures. No code written.

- 2026-07-04 — **Stage-3 tasks derived** for [coach-learning-analytics.tasks.md](coach-learning-analytics.tasks.md): plan→tasks human gate CLOSED with the **recommended R1 approach** (thread the real coach `trace_id`; emit NULL `run_ref` when no real trace exists — never the `"no-trace"` sentinel). Decomposed plan T1–T16 into a **red-first atomic checklist (Groups A–J)** grounded against first-hand precedent signatures read this session: `Attempt`/`AttemptInput` via `.omit` (`engine_entities.ts:200,214`), `DrizzleAttemptRepo` constructor `{ db, newId?, now? }` (`drizzle_attempt_repo.ts`), `EngineDb.insertAttempt`/`listMisses` on the full interface — NOT `ReadableEngineDb` (`engine_db.ts:62,96–98`), `EnginePortBag.attemptRepo` wiring + `buildEngineAdapters` (`composition_engine.ts:55,105`), the 9-port barrel (`ports/engine/index.ts`). Build order = the plan's dependency chain: **A** wire+discriminated payload → **B** table both dialects + `ENGINE_TABLE_NAMES` lock → **C** `EngineDb` interface + both impls (step_index monotonicity the load-bearing red-first test, `(episode_id,step_index)` unique index for R2) → **D** port+repo+barrel → **E** composition (server+browser) → **F** conformance vs both impls + serving read-seam isolation lock (H1/B2) → **G** 3 emit sites (R1 threaded into **G4a** before coach emit) → **H** server `task_id`-into-trace (parallel pytest) → **I** ADR-0016 draft + ADR-0006 12th-row amendment (ratifies before merge; ratchet enforces file exists) → **J** D5 stub + full-gate close. Every task names its RED assertion + GREEN impl; failure-path first (TAP-4). tasks→implement gate pending (ADR-0016 I1 drafted first). No code written yet.

- 2026-07-03 — Added [coach-learning-analytics.spec.md](coach-learning-analytics.spec.md) (SDD Stage-2 EARS spec for D1: `learning_event` append-only table both dialects + `LearningEventRepo` 12th write port + three-call-site emit seam + episode boundaries (quiz via `SessionRepo`, test/coach self-emitted) + the D0 `elapsed_ms` fix + FR-5.1 crosswalk source). Clarify pass CLOSED with 4 evidence-grounded decisions: **C1** `run_ref`=`trace_id` (client-visible) + offline `trace_id→task_id→eval` crosswalk (task_id is NOT on the wire and is an independent uuid4 — `langgraph_runtime.py:186–188`, `eval_capture.py:37–40`); **C3 corrected** — engine write ports already exist (`AttemptRepo.record`/`QuestionRepo.save`/`Scheduler.review`/`SessionRepo`), so the ADR APPLIES the `AttemptRepo` append-only precedent rather than excepting a read-only rule (brainstorm's "first write port" was wrong); scope = behavior+episode only (`experience_*`/`feedback_*` reserved); D5 join = sibling spec. Raises **ADR-0016**. Spec→plan human gate next.

- 2026-07-03 — Added [coach-learning-analytics.brainstorm.md](coach-learning-analytics.brainstorm.md) (SDD Stage-1: learner-analytics event plane for coach + test mode, distinct from the governance + eval planes, RL-trajectory-ready schema constraints; premise audit verified the P4 gap — only `NullTelemetrySink` exists, `attempt.elapsed_ms` is a hardcoded-0 stub — and refuted trajectory-readiness; six directions D1–D6 generated). **Gate CLOSED**: G-A=D1 (event table + 12th write port + ADR), G-B=YES (D4 trajectory fields in-schema day one), G-C=(i) D0 fix + (ii) D5 job now, defer (iii/iv/v). Three binding corrections folded into §8 after a post-gate evidence check — C1: the `run_ref` coach-turn join is UNSOLVED (`task_id` is not on the wire and is an independent `uuid4()` from `trace_id` — `langgraph_runtime.py:186–188`), so the join key is the spec's #1 clarify item; C2: the persistence-defer is contingent on C1, not just G-B; C3: "first write port" must be grep-verified before the ADR leans on it. → sdd-spec next.

- 2026-07-03 — Added [coach-test-mode-governed-plane.spec.md](coach-test-mode-governed-plane.spec.md) (Phase-6 spec authoring FR-23..27 in EARS; 4 clarify decisions ratified: governed-plane-only scope / separate `test_item` table / LLM-solver key gate / TS-parse→Python-promotion importer) and expanded the plan's Phase-6 milestone stub into the 6.0–6.12 task list. Pairs with [ADR-0015](../adr/0015-subject-coach-test-item-bank-blueprint-read-seam.md) (PROPOSED — ratification gates sdd-implement). Also refreshed the plan status ledger: PR #114 + #120 both MERGED, branch level with main.

- 2026-07-02 — Added [subject-coach-agent.plan.md](subject-coach-agent.plan.md) (6-phase implementation plan with status ledger: Phase 1 built + Stage-7 review closed at `a1c9a76`; 1B-10 middleware shadow wiring open) and indexed the Subject-Coach brainstorm→spec→plan family.

- 2026-06-20 — Declared `docs/plan/` an OKF bundle: added `index.md` + this `log.md` and typed frontmatter on every Concept (pure prepend, bodies unchanged). Convention in [CONVENTIONS_OKF.md](../CONVENTIONS_OKF.md); linted by `scripts/okf_lint.py`.
