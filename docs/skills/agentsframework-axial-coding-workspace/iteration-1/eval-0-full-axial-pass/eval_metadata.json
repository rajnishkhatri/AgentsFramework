{
  "eval_id": 0,
  "eval_name": "full-axial-pass",
  "prompt": "Finished open coding coach traces at .../coded_slice.jsonl (30 traces, ~21 codes). Do the axial coding pass: build the failure taxonomy to hand to rubric design. Show categories, counts, minimal pairs.",
  "assertions": [
    {"text": "Every distinct code is assigned exactly one axis (agent-behavior / environment-confound / judge-reliability)"},
    {"text": "The axial_checker.py gate was run and passed before any counts were quoted"},
    {"text": "Each agent-behavior category carries a testable binary pass/fail check, not a vibe label"},
    {"text": "truncated-reply is treated as environment-confound and excluded from agent-behavior denominators"},
    {"text": "At least one minimal pair (same prompt, divergent codes) is surfaced"}
  ]
}
