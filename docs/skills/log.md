# Skills bundle log

Chronological history of the skills bundle, newest first (ISO-8601).

- 2026-07-17 — Added `synthetic-data-pipeline`: process-checklist skill distilled from `research/Synthetic-data-pipeline-research.md` (2026-07-17). Encodes the 7-step stage-gated pipeline for coach question-bank content (demand gate → scoped generation → deterministic cascade → solve-consistency/contamination → ISO 2859-1/AQL acceptance sampling → emit → monitor), the Plane C / Plane T split (Plane T = DEFER, no consumer), the gate quick-reference table with thresholds, and the `reviewed=true`-is-earned-at-Step-5 law. Built with the skill-creator eval loop.
- 2026-07-04 — Added `agentsframework-axial-coding`: the operational companion to `llm-eval-grounded-theory` Stage 2 (as `agentsframework-open-coding` is to Stage 1). Runbook + three read-only `scripts/` (`axial_checker.py` emit gate, `axial_matrix.py` trace/occurrence counts with FR-3 confound exclusion, `axial_minimal_pairs.py`) plus `axial_cite_check.py` (exemplar-ID fidelity, the prose-emit gate). Extends `scripts/build_coach_open_code_inventory.py` with `axis` + `category` columns. Hard rule: no assertion from an unpartitioned aggregate. Derived by hand twice (GoalJudge Phase 3, eng-coach) then spec'd via SDD (`docs/plan/agentsframework-axial-coding-skill.{brainstorm,spec,plan,tasks}.md`); two skill-creator eval iterations hardened the count-unit, gradient-membership, polarity, and citation checks. Tests in `tests/skills/axial_coding/`.
- 2026-07-02 — Added the six `sdd-*` skills (`sdd-lifecycle`, `sdd-brainstorm`, `sdd-spec`, `sdd-replan`, `sdd-implement`, `sdd-converge`) operationalizing the 10-stage SDD lifecycle runbook (`docs/research/agenticengineeringplaybook/sdd_lifecycle_runbook.md`); stages 7–8 reuse `code-review` + `make check`/arch-tests rather than new skills. Same change made the mirrors mechanical: `scripts/sync_skills.py` + `make skills-sync` project this bundle to `.claude/skills/` + `.cursor/skills/` (now tracked), enforced by `tests/architecture/test_skills_mirror_parity.py`.
- 2026-06-25 — Added `memory-compaction` skill to the bundle: re-hook-first lossless compaction of Claude Code's always-loaded `MEMORY.md` index when it exceeds ~15 KB (target ≤12 KB), with bundled `analyze_memory.py` / `verify_memory.py` scripts, deeper-levers + auto-trigger references, and a SessionStart hook. Mirrors the live skill at `~/.claude/skills/memory-compaction`. Includes the packaged `memory-compaction.skill`.
- 2026-06-20 — Declared `docs/skills/` an OKF bundle: added `index.md` + this `log.md`, added `type: skill` frontmatter to all 7 `SKILL.md` Concepts. Convention pinned in [CONVENTIONS_OKF.md](../CONVENTIONS_OKF.md); linted by `scripts/okf_lint.py`.
