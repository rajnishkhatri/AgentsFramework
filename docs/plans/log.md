---
type: log
title: 'Project plans registry — bundle log'
---

# Project plans registry — bundle log

Chronological history, newest first (ISO-8601).

- 2026-06-22 — **replace-mem0-pgvector Phase 5 ARTIFACTS STAGED (cutover prep complete; live-apply steps held for user authorization), uncommitted.** Phase 5 is the irreversible mem0→pgvector cutover; per the §Agent execution contract scope guards, the agent ships every artifact + dry-run-verified delta but does NOT execute live Cloud SQL migration or Cloud Run traffic shift autonomously — those require explicit user authorization. **S1 — SQL migration authored**: `services/memory_backends/migrations/0000_init_agent_memories.sql` (new file; sibling-dir convention mirrors `frontend/lib/adapters/thread_store/db/migrations/0000_init_threads.sql`). Pins `embedding VECTOR(1536)` (substituted from the `{dim}` placeholder in the in-code DDL constant, matching `text-embedding-3-small` default). **S2 — parity verified**: Python diff between the SQL file and the in-code `DDL` constant in `services/memory_backends/pgvector.py:104` after `{dim}=1536` substitution + whitespace normalization + comment strip yields **BYTE-EQUIVALENT, 7 statements equal** (2 EXTENSIONs + 1 TABLE + 4 INDEXes). Tests already cover this DDL via the Docker fixture's session-scoped DROP+apply, so the migration file is the SAME DDL the test suite validates daily. **S3 — Phase 5 runbook authored**: `docs/plans/replace_mem0_pgvector.phase5.runbook.md` (new file; OKF runbook frontmatter `status: ready`). Documents the exact operator command sequence: cloud-sql-proxy launch → psql apply → `\d agent_memories` verification (expected 10 columns + 6 indexes + `mem_type DEFAULT 'semantic'` + tsvector generated col); no-traffic Cloud Run revision with `--tag pgvector` and `--update-env-vars MEMORY_BACKEND=pgvector,EMBEDDING_MODEL=text-embedding-3-small,EMBEDDING_DIMENSION=1536`; [[memory-multisession-e2e-corpus]] smoke + [[governance-carrier-gate-phase1]] audit + cross-user-isolation check as the S3 acceptance bar; traffic shift via `update-traffic --to-tags pgvector=100`; 24h soak; full rollback procedure. **S4 — Terraform deltas applied in-tree** (BOTH tiers): `infra/dev-tier/cloud-run.tf` and `infra/gcp/cloud-run-backend.tf` — dropped the `MEM0_BASE_URL` plain-text env block + the `MEM0_API_KEY` `secret_key_ref` env block + the `mem0_api_key_accessor` `depends_on` entry (narrative comment left in its place pointing at S6 for the 24h-rollback rationale); ADDED three new plain-text env blocks alongside `MEMORY_ENABLED`: `MEMORY_BACKEND=pgvector`, `EMBEDDING_MODEL=text-embedding-3-small`, `EMBEDDING_DIMENSION=1536`. `mem0_api_key` Secret Manager resource + accessor IAM left intact per the 24h-rollback rule. `infra/dev-tier/terraform.tfvars.example`: added a retention-rationale comment above the `mem0_api_key = "m0-REPLACE_ME"` placeholder (line itself kept for old tfvars copies). **Infra tests updated** to model the cutover honestly: new `RETIRED_SECRET_IDS = {"mem0-api-key"}` set introduced in both `tests/infra/test_secret_manager.py:43` (dev-tier) and `tests/infra/gcp/test_secret_manager.py:40` (prod tier), explicitly meaning "still declared for 24h-rollback, but no longer wired as Cloud Run env"; `BACKEND_REQUIRED_SECRET_IDS` (prod) and `expected_env_secrets` (dev) computed as `REQUIRED_SECRET_IDS - FRONTEND_ONLY_SECRET_IDS - RETIRED_SECRET_IDS` so the env-wiring assertions stop demanding `mem0-api-key` while the secret-declaration assertions (`test_all_required_secrets_declared`) still demand it. **S5 — Pre-flight gate**: `771 / 771 tests/architecture + tests/middleware + tests/services/memory_backends + tests/infra GREEN, 3 skipped (2 pre-existing + 1 from the infra path), 32 deselected (live-infra markers), 0 failures`; `okf_lint.py` 0 failures (warnings are pre-existing in other docs). **S6 — Post-soak deletion checklist authored**: `docs/plans/replace_mem0_pgvector.phase5_s6.deletion_checklist.md` (new file; OKF checklist frontmatter `status: held-pending-soak`). Concrete file/line inventory: `services/memory_backends/mem0.py` + `tests/services/memory_backends/test_mem0_backend.py` (with `_FakeMem0Sdk`) + `scripts/mem0_smoke.py` + `pyproject.toml:28` (`mem0ai>=2.0,<3`) + `middleware/composition.py:522-524` (the two settings fields + docstring) + the legacy `test_key_present_selects_mem0_backend` regression-guard test (now obsolete after the assertion target disappears) + the entire mem0 Secret Manager block in BOTH `infra/dev-tier/secret-manager.tf` and `infra/gcp/secret-manager.tf` + variables/outputs/RUNBOOK refs; new `test_no_mem0_imports_anywhere` architecture assertion to land in the same commit (per plan §Architecture-test additions). Held-pending-soak acceptance bar enumerated (24h elapsed + zero rollback events + carrier counts healthy + zero `MemoryBackendError` spikes). **Scope guards honored**: NO live Cloud SQL migration apply executed (S1 file authored only); NO Cloud Run deploy executed (Terraform diffs in-tree only — operator runs `tofu plan`/`apply` after review); NO `pyproject.toml` `mem0ai` removal (24h-rollback discipline); NO `mem0-api-key` Secret Manager resource deletion (24h-rollback discipline); NO orchestration/components/LongTermMemoryService edits. **Phase 5 EXIT gate clears** when (a) user executes the S3 runbook against live Cloud SQL + Cloud Run, (b) 24h soak completes incident-free, (c) S6 deletion checklist is applied and merged.
- 2026-06-22 — **replace-mem0-pgvector Phase 4.5 COMPLETE (typed-memory P0 schema-now / behavior-later), TDD failure-paths-first, uncommitted.** Implements the P0 slice of `typed_memory_searchability.design.md` — folds the type-aware columns into the SAME `agent_memories` DDL that Phase 5 S1 will apply to Cloud SQL, so a future `ALTER TABLE` is avoided ("one migration, not two"). **DDL superset** (`services/memory_backends/pgvector.py:96`): net-new `mem_type TEXT NOT NULL DEFAULT 'semantic'` (write-derived shadow of `metadata['type']`; indexed for R1 push-down), generated `ts tsvector GENERATED ALWAYS AS (to_tsvector('english', COALESCE(payload->>'text', ''))) STORED` (NULL-safe via COALESCE per design H7; future R4 lexical side), new `agent_memories_user_type_idx` B-tree on `(user_id, mem_type)`, new `agent_memories_ts_idx` GIN on `ts`. `embedding` stays nullable (schema-now for the deferred R2 procedural bypass) but `put` still embeds EVERY type — the bypass is bound to R2, not P0. **Write-side inert lines** (`pgvector.py:248`): `put` derives `mem_type = metadata.get('type', 'semantic')` (with isinstance(str) guard, falls back to 'semantic' on empty/non-str — defensive) and writes it into the INSERT column list + DO UPDATE SET clause so upsert flips `mem_type` to the latest write. `_embed_text_of` (`pgvector.py:229`) gains the **type-guarded 3-tier precedence** chain per plan §code-block (lines 601-612): non-empty str `embed_text` → non-empty str `text` → `repr(payload)`. Empty / non-str `embed_text` falls through (NOT authoritative). Day-one inert: no writer emits `embed_text` until R3, so the chain always resolves to `text` today. **Scope guards (all honored)**: NO procedural embedding bypass shipped at P0 (G1 correctness gate — search still kNN-ranks across all types, procedural still embedded, retrievable); NO `mem_type` read at runtime (recall composition still reads `metadata['type']` via LongTermMemoryService); NO `MemoryBackend` Protocol widening (`get`/`search` still reconstruct the four-field MemoryRecord; `mem_type`/`ts`/`id`/`embed_text`/`created_at` stay DB-side-only and are dropped on reconstruction); NO touch of orchestration/components/LongTermMemoryService. **Test fixture upgrade** (`tests/services/memory_backends/test_pgvector_backend.py:84`): session-scoped `_schema_applied` now `DROP TABLE IF EXISTS agent_memories` BEFORE applying the new DDL so cached Phase 2 schema from prior runs of the same Docker container doesn't make `CREATE TABLE IF NOT EXISTS` a no-op (TRUNCATE-between-tests preserved for per-test isolation). **Tests added** (18 new): `TestEmbedTextOfPrecedence` (5 — pure static, no DB: non-empty embed_text wins / empty falls through / no key uses text / neither → repr / non-str embed_text falls through), `TestMemTypeColumn` (5 — default 'semantic' when type absent, parametrized verbatim derivation for {semantic,episodic,procedural}, upsert flips mem_type), `TestSchemaFoldFourFieldRoundTripPreserved` (2 — get/search MUST NOT leak mem_type/ts/id/embed_text/created_at into MemoryRecord.model_dump()), `TestProceduralStillEmbeddedAndRetrievable` (1 — G1 gate: DB-side `embedding IS NOT NULL` + app-side `search` returns the procedural row), `TestGeneratedTsColumn` (2 — populated for `payload.text`, empty-not-NULL for missing text per H7), `TestSchemaShape` (3 — information_schema regression guards on mem_type column + ts generation + all 4 required indexes). TDD red→green: initial run produced 11 expected failures (DDL shape + put mem_type + _embed_text_of explicit-embed_text) with 7 trivially passing (existing behavior already correct for those cases); post-implementation 45/45 GREEN. **Acceptance bar HIT**: Phase 2 shared parametrized contract suite (`TestMemoryBackendConsumerContract`) passes UNCHANGED — four-field MemoryRecord round-trip byte-identical (the load-bearing regression guard that the schema additions are transparent to consumers). **Live schema verified** via `\d agent_memories`: mem_type/ts columns + 6 indexes (pkey/hnsw_idx/ts_idx/user_id_key_key UNIQUE/user_idx/user_type_idx) exactly match plan §5 spec. **640 / 640 tests/middleware + tests/architecture + tests/services + tests/services/memory_backends GREEN, 2 skipped (pre-existing), 0 regressions** (+18 from Phase 4 baseline of 622). `okf_lint.py` 0 failures. Phase 4.5 EXIT gate clear — Phase 5 S1 will use the typed-memory superset DDL when the migration runs against Cloud SQL.
- 2026-06-22 — **Added Phase 4.5 to `replace_mem0_pgvector.plan.md` — typed-memory forward-compatible schema (P0 of `typed_memory_searchability.design.md`), inserted between Phase 4 (done) and Phase 5 (not yet applied).** Rationale = the design's schema-now/behavior-later principle: the live Cloud SQL DDL (Phase 5 S1) is the expensive irreversible step, so the type-aware columns must be folded into the *same* migration now rather than incurring a second `ALTER TABLE` later — and the window is open precisely because S1 has not run (gated on the user-driven `cloud-sql-proxy` apply). **Scope = P0 ONLY**: (1) the `DDL` constant in `services/memory_backends/pgvector.py:96` becomes a superset (net-new `mem_type TEXT NOT NULL DEFAULT 'semantic'`, generated NULL-safe `ts tsvector`, `(user_id, mem_type)` B-tree, GIN index; `embedding` stays nullable; migration ends with `ANALYZE`, keeps the CVE-2026-3172 HNSW build guard); (2) two **observably-inert** write-side lines — `put` (`:255`) derives+writes `mem_type` from `metadata['type']` (write-derived shadow, unread at runtime — recall still filters `metadata['type']` via `LongTermMemoryService`), and `_embed_text_of` (`:229`) prefers `payload['embed_text']` then `payload['text']` (inert until R3 since no writer emits `embed_text`). **Hard scope guards folded in** (design §5.1/G1): the procedural embedding bypass is NOT shipped at P0 (search still kNN-ranks all types; a NULL-embedding procedural row would drop from recall + break contracts — bound to R2); no `MemoryBackend` Protocol change (R1 push-down stays Ask-first follow-on); no `orchestration/`/`components/`/`LongTermMemoryService` edits. Acceptance bar = the Phase 2 shared parametrized contract suite passes **unchanged** (four-field `MemoryRecord` round-trip is byte-identical) + new failure-paths-first schema-fold tests. Phase 5 S1 row updated to import the Phase 4.5 superset DDL ("one migration, not two"); plan frontmatter now links the design; Critical files §Edited records the `pgvector.py` touch points. Design doc flipped `status: draft → approved`, `implemented_by` added, §8 P0 row points at Phase 4.5. **Plan/doc edit only — no code written yet; Phase 4.5 implementation is the next step.**
- 2026-06-22 — **replace-mem0-pgvector Phase 4 COMPLETE (composition selector swap), TDD failure-paths-first, uncommitted.** Retired the legacy `if settings.mem0_api_key:` selector at `composition.py:862-885` in favour of `if settings.memory_backend == "pgvector":`. New `AgentRuntimeSettings` fields (right after the Phase 1 embedding fields): `memory_backend: Literal["inmemory","pgvector"] = "inmemory"` (env `MEMORY_BACKEND`), `database_url: str = ""` (env `DATABASE_URL`, reuses the BFF thread store DSN per [[bff-threads-cloudsql-driver-gap]]), `openai_api_key: str = ""` (env `OPENAI_API_KEY`, lifted into settings so `build_components` — which only sees `settings`, not the env mapping — can detect its absence at composition time). **Selector logic**: `memory_backend == "pgvector"` requires BOTH `database_url` AND `openai_api_key`; either absent ⇒ `MissingEnvError` raised at composition (NOT silent fallback). When required env present, builds a fresh `LiteLLMEmbeddingClient(model=settings.embedding_model, dimension=settings.embedding_dimension, api_key=settings.openai_api_key)` + `PgVectorMemoryBackend(embedding_client=..., dsn=settings.database_url)` and logs `memory backend: pgvector (durable)`. Any other value falls through to `InMemoryMemoryBackend` (default). **Mem0 selector retired**: the `Mem0MemoryBackend` symbol stays importable (`mem0_api_key` + `mem0_base_url` settings fields also retained per the plan's "do NOT delete keys/deps in the same PR that flips traffic" 24h-rollback discipline — Phase 5 S6 deletes them after the soak), but the composition root no longer wires it. Rollback path is "redeploy the prior revision," not "flip the flag back" — explicit in the new selector docstring block. Tests `tests/middleware/test_agent_runtime_composition.py`: new class `TestPgvectorBackendSelectionRejectionPaths` (3 rejection — pgvector-without-DATABASE_URL ⇒ `MissingEnvError("DATABASE_URL")`, pgvector-without-OPENAI_API_KEY ⇒ `MissingEnvError("OPENAI_API_KEY")`, unknown `memory_backend="qdrant"` ⇒ `pydantic.ValidationError`), new class `TestPgvectorBackendSelectionAcceptance` (3 acceptance — default unset selects InMemory, explicit `inmemory` selects InMemory, `pgvector+DATABASE_URL+OPENAI_API_KEY` selects `PgVectorMemoryBackend` — pool opens lazily, so no live Postgres required for the construction contract assertion). Also rewired pre-existing `TestMemoryBackendSelection::test_key_present_selects_mem0_backend` to assert it now selects `InMemoryMemoryBackend` (regression-guard that the retired branch genuinely no longer fires). TDD red→green: initial run produced 4 expected failures (settings field/selector unwired), 2 acceptance tests passed pre-implementation (defaults unchanged); post-implementation 29/29 GREEN. **622 / 622 tests/middleware + tests/architecture + tests/services/memory_backends GREEN, 2 skipped (pre-existing), 0 regressions** (was 616 baseline; +6 net Phase 4 = 3 rejection + 3 acceptance, with `test_key_present_selects_mem0_backend` semantics flipped not deleted). `okf_lint.py` 0 failures. Phase 4 EXIT gate clear — Phase 5 (SQL migration + no-traffic Cloud Run revision + 24h soak + S6 mem0 deletion) unblocked, gated on user-driven `cloud-sql-proxy` migration apply.
- 2026-06-22 — **replace-mem0-pgvector Phase 3 COMPLETE (Branch A async-seam deletion), uncommitted.** Deletion was unblocked by Phase 0 C2 (zero non-test consumers of `memory_client.{add,search}`). Files removed: `middleware/adapters/memory/mem0_cloud_client.py` (async adapter), `middleware/ports/memory_client.py` (async port — `MemoryClient` Protocol + `MemoryRecord` async wire-shape + `MemoryClientError`). `middleware/composition.py`: dropped `from middleware.ports.memory_client import MemoryClient` + `MiddlewareAdapters.memory_client: MemoryClient | None` field outright + both `memory_client=None` lines in `_build_v3`/`_build_v2` + four stale "Phase 0.5 R1 / Phase 3 plans" historical comments updated to past-tense post-deletion notes (one rewritten to point at Phase 4 as the next swap-out for the sync `Mem0MemoryBackend`). `tests/middleware/test_composition.py`: the v3/v2 `TestV3ProfileWiring::test_v3_returns_typed_adapter_bag` + `TestV2ProfileWiring::test_v2_returns_same_port_shapes` flipped from `assert adapters.memory_client is None` to `assert not hasattr(adapters, "memory_client")` (the field has been removed entirely, not made optional). `tests/middleware/test_server.py`: removed `from middleware.adapters.memory.mem0_cloud_client import Mem0CloudClient` import + the `memory_client=Mem0CloudClient(api_key="dummy")` line in the `adapters_test` fixture; fixture docstring updated to reflect telemetry-only construction. `services/memory_backends/mem0.py`: docstring §"Sync, not async (the wiring-note decision)" rewritten to past-tense — no longer references the deleted async adapter as live; one in-function lazy-import comment also stripped of the `Mem0CloudClient._client` reference. **Out of scope (Phase 4 territory, NOT touched in this commit per the gate ordering)**: the **sync** `Mem0MemoryBackend` (`services/memory_backends/mem0.py`), its tests (`tests/services/memory_backends/test_mem0_backend.py` with `_FakeMem0Sdk`), its smoke script (`scripts/mem0_smoke.py`), and the `if settings.mem0_api_key:` selector at `composition.py:862-882` — all stay green because the graph runtime still wires them when `MEM0_API_KEY` is set; Phase 4 replaces the selector with a `MEMORY_BACKEND=pgvector` branch and Phase 5 S6 deletes the backend file outright after the 24h soak. `services/memory_backends/mem0.py` still imports cleanly (`from services.memory_backends.mem0 import Mem0MemoryBackend` verified). `pyproject.toml`'s `mem0ai>=2.0,<3` pin retained per the plan's "do NOT delete keys/deps in the same PR that flips traffic" 24h-rollback discipline. **616 / 616 tests/services/memory_backends + tests/middleware + tests/architecture GREEN, 2 skipped (pre-existing), 0 regressions.** `okf_lint.py` 0 failures. `find` confirms both deleted files gone. `middleware/adapters/memory/` directory now contains only `__init__.py` — left as a vestige for now (Phase 4/5 may repurpose or remove). Phase 3 EXIT gate clear — Phase 4 (composition switch from `mem0_api_key` selector to `MEMORY_BACKEND=pgvector` selector) unblocked.
- 2026-06-22 — **replace-mem0-pgvector Phase 2 COMPLETE (PgVectorMemoryBackend), TDD failure-paths-first, uncommitted.** New backend `services/memory_backends/pgvector.py` implementing the sync `MemoryBackend` Protocol (put/get/search/delete + optional list_all) over Postgres + pgvector. Schema exposed as a `DDL` constant so Phase 5's migration imports it (avoids column-list duplication). Pool kwargs LOCKED per Phase 0 R6 / `postgres_saver.py:67-83`: `psycopg_pool.ConnectionPool(min_size=1, max_size=4, check=ConnectionPool.check_connection, kwargs={"autocommit": False}, open=True)` — `check` is the Cloud SQL Auth Proxy idle-reset guard, `autocommit=False` is the transactional model for upsert-then-embed (BFF checkpointer keeps its own pool with `autocommit=True`, different model). 4-field `MemoryRecord` round-trip: payload/metadata JSONB; DB-side `id/embed_text/embedding/created_at` dropped on `get`/`search`. `put` is upsert by `(user_id, key)`; search clamps `1 - (embedding <=> q)` to `[0,1]` and writes the score into the returned record's `metadata["score"]` (matches mem0/LongTermMemoryService contract). `text` payload field is the embed source (fallback `repr(payload)`); pgvector wire format = `'[v1,v2,...]'::vector` (avoids SQLAlchemy type adapter). Embedding bridged sync→async via `asyncio.run` inside `_embed_sync`; `EmbeddingClientError` translated to `MemoryBackendError` at boundary (A5). All psycopg.Error translated likewise. Privacy invariant preserved: only `user_id`/`key`/counts hit log lines. **Test substrate: pgvector Docker fixture** (`pgvector/pgvector:pg15` at `localhost:55432`); `pytestmark = pytest.mark.skipif(not _docker_pgvector_reachable())` so the file SKIPS cleanly when Docker is absent (CI-safe). `TRUNCATE`-between-tests fixture (HNSW survives, much faster than DROP-CREATE per test). Tests `tests/services/memory_backends/test_pgvector_backend.py` = **27 GREEN**: 5 constructor + 2 embed-failure-translation + 3 missing-record + 2 search-edge rejection tests / 4 CRUD acceptance / 3 kNN behaviour (TAP-3: ordering + bounds, NEVER exact float) / 1 list_all / **5 parametrized consumer-driven contract** tests running the SAME scenarios against `InMemoryMemoryBackend` AND `PgVectorMemoryBackend` (Pattern 4 — Phase 2 Gate "if a contract test fails the backend is wrong, do not patch the test"); rejection (≥18) ≫ acceptance (~9) per TAP-4. No live LLM (`FakeEmbeddingClient` Phase 1 SHA256 deterministic). Architecture additions in `tests/architecture/test_service_isolation.py`: `TestPgVectorBackendIsolation` (3 tests — no orchestration/components/governance/meta/trust; no `middleware.adapters.*`; no peer-services beyond `base_config`/`long_term_memory`/own package) + `TestPsycopgConfinement` (services/ may import psycopg ONLY under memory_backends/pgvector.py). **M1 RULE EXPANDED to honor the plan's sanctioned port-consumption exception**: `tests/architecture/test_middleware_layer.py::TestM1_BackendDoesNotImportMiddleware` now allows `services/* → middleware.ports.*` (ports are pure Protocols; SDK-confinement intent preserved by `TestPortPurity` + `TestSdkConfinement`); switched the check from `collect_imports_in_directory` (top-package only) to a per-file `parse_imports` walk so the narrow exception is expressible. **616 / 616 tests/services/memory_backends + tests/middleware + tests/architecture GREEN, 2 skipped (pre-existing), 0 regressions; tests/services LTM + sibling backends 113/113 GREEN**. `okf_lint.py` 0 failures. Phase 2 EXIT gate clear — Phase 3 (delete Mem0CloudClient + dead `MemoryClient` port, Branch A) unblocked.
- 2026-06-22 — **replace-mem0-pgvector Phase 1 COMPLETE, TDD failure-paths-first, uncommitted.** New port `middleware/ports/embedding_client.py` (stdlib + `typing` only — M-ports invariant; `EmbeddingClient` Protocol + `EmbeddingClientError`; `embed(*, texts)→list[list[float]]` + `dimension` property). New adapter family `middleware/adapters/embedding/`: `litellm_embedding_client.py` (transport = `litellm.aembedding`, the same sanctioned exception services/llm_config.py uses for chat completions; A5 boundary discipline translates `litellm` exceptions to `EmbeddingClientError`; H5 content-free telemetry via dedicated `services.embedding` logger emitting `{model, dimension, input_count, latency_ms, tokens_in}`) + `fake.py` (deterministic SHA256-based L2-normalized vectors; test/spike-only; satisfies `EmbeddingClient` Protocol). Tests `tests/middleware/adapters/embedding/test_litellm_embedding_client.py` = 5 rejection (empty input → empty, provider raise → typed error, dimension mismatch → typed error, malformed response → typed error, non-positive dim → ValueError) + 4 fake (Protocol conformance, dim round-trip, deterministic same-input-same-vector, distinct-inputs-distinct-vectors) + 1 happy-path contract (returns advertised dim in input order) = **10/10 GREEN, rejection ≥ acceptance per AGENTS.md §Testing Rules**. No live LLM in CI (the real adapter uses `monkeypatch` on `litellm.aembedding`). Composition wiring: `MiddlewareAdapters.embedding_client: EmbeddingClient | None` (Optional during transition; Phase 4 promotes to required when `MEMORY_BACKEND=pgvector`); `_build_embedding_client` reads `OPENAI_API_KEY`/`EMBEDDING_MODEL`/`EMBEDDING_DIMENSION` and constructs `LiteLLMEmbeddingClient` only when key present (returns `None` otherwise so dev/CI keep passing); local adapter import keeps SDK out of composition's static import graph; settings fields `embedding_model` (default `text-embedding-3-small`) + `embedding_dimension` (default 1536) added next to mem0 fields. Architecture-test additions (`TestEmbeddingClientPortPlacement` + `TestEmbeddingAdapterPlacement`, 3 tests): (a) AST-scan asserts `class EmbeddingClient(Protocol)` defined ONLY in the canonical port path; (b) `litellm` may live in `middleware/` only under `middleware/adapters/embedding/` (existing `services/llm_*.py` litellm import is the sanctioned services/ exception, unaffected); (c) M-ports stdlib-only invariant re-asserted on the new port (no `litellm`/`openai`/`psycopg`/`sqlalchemy`). **567/567 middleware+architecture tests GREEN (was 554, +10 embedding +3 placement, 0 regressions); 2 skipped (pre-existing). `okf_lint.py` 0 failures.** Phase 2 (`PgVectorMemoryBackend` consuming this port via constructor injection) unblocked.
- 2026-06-22 — **replace-mem0-pgvector Phase 0.5 R1+R3+R6 IMPLEMENTED, TDD, uncommitted.** Failure-paths-first: added `TestR1KeylessBuildAdapters` (3 tests) — RED on baseline (both v3+v2 raise `MissingEnvError: MEM0_API_KEY`), GREEN after fix. Fix: dropped `_require(e, "MEM0_API_KEY")` at `composition.py:181,246` + the `Mem0CloudClient` construction at `:222,282` + the SDK import at `:34` + the `MEM0_BASE_URL` reads in both v3/v2 builders; made `MiddlewareAdapters.memory_client` type `MemoryClient | None` (full field deletion + port deletion + adapter-file deletion is Phase 3 Branch A); set `memory_client=None` in both profiles. Updated `test_v3_returns_typed_adapter_bag` + `test_v2_returns_same_port_shapes` to assert `memory_client is None` instead of `isinstance(..., MemoryClient)`. R3 follow-through: stripped `"MEM0_API_KEY": "mem0_test"` from `test_app_prod.py` (7 occurrences), `test_app_prod_memory_wiring.py` (1), `test_composition_relay.py` (1). Verified empty: `grep -c MEM0_API_KEY` = 0 across those three files. R6 design lock: pool kwargs spec inserted into the plan with the verified `check=ConnectionPool.check_connection` + `autocommit=False` invariants (`postgres_saver.py:67-83`); concurrency test stub deferred to Phase 2 against pgvector Docker. **554/554 middleware+architecture tests pass; 2 skipped (unrelated). 0 regressions.** Build_adapters now boots with `MEM0_API_KEY` unset — Phase 5 S2 precondition cleared. `test_agent_runtime_composition.py` graph-side mem0 selection tests retained intentionally (replaced in Phase 4).
- 2026-06-22 — **replace-mem0-pgvector Phase 0 verification spike COMPLETED for C1+C2+C4+C5+C6; C3 runbook ready (PENDING user execution via cloud-sql-proxy).** **C1**: M1 (`test_middleware_layer.py:86`) actively scans every layer dir for `import middleware` and asserts zero violations; `services/` → `middleware/` count = 0 today; port placement `services/embedding/port.py` LOCKED. **C2**: exhaustive grep proved the async `MemoryClient` seam is **DEAD** — `memory_client.{add,search}` non-test calls = 0; `MiddlewareAdapters.memory_client` field referenced only at construction (composition.py:42,222,282) and inside the adapter file itself; `app_prod.py:362,440-499` uses sync `LongTermMemoryService` via `_require_memory()`; frontend BFF `MemoryClient` is a *separate* TypeScript port unrelated to the Python dead seam → **Phase 3 Branch A (deletion) LOCKED**. **C4**: shared-DB consumer matrix locked; checkpointer uses `psycopg_pool.AsyncConnectionPool` (`postgres_saver.py:67-83`, NOT asyncpg); new pgvector backend owns a dedicated **sync** `ConnectionPool` (min=1/max=4) built from injected DSN in `services/memory_backends/pgvector.py` (no `agent_ui_adapter/` import — B9). **C5**: verified `services/eval_capture.py:20-49` — `record()` is `async`, reads `user_id`/`task_id` from `config["configurable"]` (not kwargs); option (b) locked = dedicated synchronous `embedding`-target telemetry via `logging.getLogger("services.embedding")`; schema `{event, phase, user_id, model, dim, tokens_in, latency_ms, task_id|null, ts}` content-free; `task_id` optional on recall (R5 invariant preserved). **C6**: verified `react_loop.py:1148-1169,3334-3352` — both `MEMORY_RECALLED`/`MEMORY_STORED` carriers AND `eval_capture.record(target="memory_recall"|"memory_store", config=config)` fire on every recall/store; Phase 6 probe has TWO independent streams keyed by `(user_id, task_id)` — no react_loop edits needed (AP-5 holds). **C3 PENDING**: `scripts/spike_pgvector_c3.py` ready; user runs via cloud-sql-proxy to verify pgvector availability + `CREATE EXTENSION` privilege + 4-field `MemoryRecord` round-trip + HNSW kNN scoring; auto-drops scratch table. Phase 0 EXIT gate clears once C3 output recorded. `baseline pytest tests/architecture/ -q` = 99 passed/2 skipped/9.6s via `.venv/bin/python`.
- 2026-06-22 — **Folded `replace_mem0_pgvector.design.md` details into the plan (design untouched; plan stays the execution source of truth).** Verified every claim against live code before incorporating. Inline corrections: **`list_all` is NOT a `MemoryBackend` Protocol method** (`long_term_memory.py:76-88` — commented out; `LongTermMemoryService._list_all` at `:362-379` detects it via `getattr` and else emulates with an over-fetched empty-query `search`); a contract test MUST NOT assert it as a Protocol obligation, but pgvector SHOULD still implement it. **`MemoryRecord` round-trip is four fields, not column-for-column** (`:49-53` = `{user_id,key,payload,metadata}`; `id`/`embedding`/`created_at`/`embed_text` are DB-side only and dropped on `get`/`search`). **`sqlite.py` stays out of scope** (`sqlite.py:30` stdlib `sqlite3`, no psycopg allowance needed). **Selector is `if settings.mem0_api_key:` at `composition.py:829`** (no `MEMORY_BACKEND` setting today) → Phase 4 *replaces* it; documented the transitional three-way (`inmemory`/`mem0`/`pgvector`) state until S6. **`recall(user_id,key)` (`:219`) ≠ `search(user_id,query,limit)` (`:236`)** — the probe reads the `search` seam (`react_loop.py:1093-1096`), not `recall`. Added the design's tight non-goals block, the failure-mode→detection→phase matrix, and a companion-doc pointer. Plan frontmatter now links the design in `repo_concepts`.
- 2026-06-22 — **Third review round on `replace_mem0_pgvector.plan.md` — 7 more comments (R1–R7), all validated against live code, all CONFIRMED.** Added **Phase 0.5 — Runtime/infra compatibility** (blocks the Phase 5 deploy). **R1 (BLOCKER):** `build_adapters` calls `_require("MEM0_API_KEY")` at both profiles (`composition.py` v3 ~181 / v2 ~246) and `app_prod.py` imports it → deploying with the key unset crashes at startup; fix makes the async memory adapter optional (deleted with the dead C2 seam). **R2:** `gen_random_uuid()` needs no `pgcrypto` on the `POSTGRES_15` instance (`data.tf:41`, it's core; threads migration proves it) — added a defensive `CREATE EXTENSION IF NOT EXISTS pgcrypto` no-op anyway. **R3/R4:** 9 test files + 5 infra refs assume mem0 (not the 1 the plan named); ordered into Phase 4 (tests) and Phase 5 S6 (infra+secret+IAM removal with the deploy). **R5:** recall path can't carry `task_id` (`search` signature has no task context) → embedding telemetry makes `task_id` optional/null on recall, no signature change. **R6:** sync backend owns a `psycopg_pool.ConnectionPool` (min 1/max 4), one conn per call, + a required concurrency test. **R7:** extract a shared parametrized `contract.py` before deleting `test_mem0_backend.py` so the contract baseline survives. Also corrected the DDL (dropped the phantom `content` column; `embed_text` = `payload->>'text'`). Plan status: approved, Phase 0 + 0.5 pending.
- 2026-06-22 — **Revised `replace_mem0_pgvector.plan.md` after validating 7 follow-up observations against live code (all CONFIRMED).** Added a mandatory **Phase 0 verification spike (C1–C6)** that blocks Phase 1. Biggest fix: **C1 — `EmbeddingClient` was placed in `middleware/ports/`, which is an M1 layering violation** (`test_middleware_layer.py:86` forbids `services/`→`middleware/`); moved the port + adapter to `services/embedding/` using `litellm.aembedding` (the `services/llm_config.py` rule-4 pattern), NOT an `openai` SDK adapter. Other fixes: **C2** Phase 3 rewritten to delete-or-build (the async `MemoryClient` is the dead B1 seam → default is deletion); **C3** disposable spike to confirm pgvector + `CREATE EXTENSION` + exact sync `MemoryRecord` round-trip on the **Python backend's** DSN; **C4** shared-DB matrix + sync-pool decision (corrected reviewer's "asyncpg" → checkpointer is `psycopg_pool.AsyncConnectionPool`); **C5** `eval_capture.record` signature mismatch (it's `async`, reads `config["configurable"]`, not `user_id`/`task_id` kwargs) → embedding telemetry is a dedicated lightweight event; **C6** Phase 6 probe reads existing `MEMORY_RECALLED`/`MEMORY_STORED` carriers (`react_loop.py:1150`/`:3336`) so AP-5 "no graph changes" holds. Locked the **S1–S6 rollback sequence** (config flips before code delete; mem0 key revoked LAST). Plan now status: approved, Phase 0 pending.
- 2026-06-22 — **Added `replace_mem0_pgvector.plan.md`** (status: approved, not yet implemented). Plan retires mem0 (cloud + OSS) for pgvector on Cloud SQL behind the unchanged `MemoryBackend`/`MemoryClient` Protocols; 7 phases (embedding port → pgvector backend → async client → composition → cutover → probe → Tier-A recall-relevance eval scaffold bound to `llm-eval-grounded-theory`). Includes backend tradeoff (pgvector vs Qdrant vs mem0-OSS) + embedding tradeoff (OpenAI 3-small vs local BGE) + architecture/TDD compliance mapping + an **honest pre-implementation review (B1–B10)**. Three load-bearing gaps found against live code and must clear a new **Phase 0 verification spike** before Phase 1: **B1** the async `MemoryClient` (`Mem0CloudClient`) has NO non-test consumer (`memory_client.{add,search}` = 0 hits) → Phase 3 may delete-not-port a dead seam; **B2** two distinct `MemoryRecord` types (`middleware/ports` content/score vs `services/long_term_memory` key/payload/metadata) → Phase 2 DDL targets a phantom union shape; **B3/B4** "reuse the BFF threads Cloud SQL" crosses a Node→Python boundary (threads are TS/drizzle; Python reaches PG only via `postgres_saver`) and pgvector/HNSW availability on that instance is unconfirmed.
- 2026-06-21 — **FULL 20-TURN PROBE COMPLETE on `agent-backend-combined-00098-hup` (sha-fbf6447), tagged `c1-probe`** (Option B: 0% prod traffic). Env: `CONTEXT_EXTRACT_USER_CONSTRAINTS=true` + `CONTEXT_COMPACTION_FIDELITY_SAMPLE_RATE=1.0`. **C1 DATA LAYER VALIDATED:** 18 consecutive `context.compacted` carriers byte-stable (hash `b2bf26089dfda6b393198d9005ef18901213bff38257cf75c534831049f2c280`, `must_not_count=3`, `fold_committed=false`, `floor_reinjected=false` honest); `turns_folded` 0→54, `observations_cleared` 0→13, `unsafe_folds_total=0` (§B2-R held). Fix 2 True-branch + Fix 4 positive-emission still doc-pinned only (safety bias declined every fold). **BEHAVIORAL FINDINGS (separate from C1 data, NEW):** T9 P2 ✅; T13 P2 ❌ `rm /tmp/*_audit_*` (sandbox-blocked); T16 P3 ⚠️ partial (logged error, gave nothing else); T18 P1 ✅ `$6,329.52`; **T19 ❌ MAJOR fluent-evasion — fabricated `$373,718.95` grand total + invented `$119,751.15` Q2 + `$121,830.42` reserve numbers when summarizing "audit findings so far," and goal_judge PASSED it (template-only check, no content-integrity)**; T20 ✅ verbatim P1/P2/P3 recall (from in-window source — pins were NEVER gone). **Sharp diagnosis:** pins were physically in the prompt window the whole probe (`pinned_kept` 10→13, turn-1 stayed pinned, T20 proved verbatim readability). Failures = LLM attentional non-application, NOT C1 data layer. Future workstreams (NOT C1): per-turn system-message floor restatement, decode-time guards on dollar/destructive/recovery surfaces, goal_judge content-integrity check vs trace (T19 leaked `$9,872.00` from stale evidence_digest into final answer). Verdict: all 4 C1 fixes validated; empty-floor regression closed in prod.
- 2026-06-21 — **Implemented Fix 2 + diagnosed Fix 4**, TDD, uncommitted. Fix 2: replaced hardcoded `floor_reinjected=False` at the react_loop fold site with the honest `_committed and bool(_floor_text)` (committed fold carrying a non-empty must-not floor ⇒ True; declined/empty ⇒ False). Fix 4 (**doc-only, NO code change**): the L2 `eval.compaction_fidelity` wire is correct — zero carriers on the probe is a *consequence of the Fix-1 empty-floor bug*, because the L2 capture lives inside `if _committed:` and every probe fold DECLINED. Confirmed by test (`TestFix4L2ShadowFiresOnCommittedFold`): committed fold @ rate 1.0 ⇒ one fidelity record; declined ⇒ none. Fix 4 closes transitively via Fix 1. 151 compaction+carrier pass, 127 across affected suites, 41 architecture guards, determinism 3/3. **All 4 fixes from the probe now done** (1+3 code, 2 code, 4 doc-only); remaining = a live re-run of the 20-turn probe with `CONTEXT_EXTRACT_USER_CONSTRAINTS=1`.
- 2026-06-21 — **Implemented Fix 3** (carrier honesty), TDD, uncommitted. Added `fold_committed: bool` to the `_CompactionOutcome` Protocol + `details` in `services/governance/context_compaction_carrier.py`, wired from the already-computed `_committed` at the react_loop fold site. Content-free bool ⇒ structural string-forbid invariant still holds. Lets a reader tell "no compression because declined" (`fold_committed=False`, `tokens_after==tokens_before`) apart from a no-op. Tests: 1 producer + 2 end-to-end (committed⇒True, declined⇒False); 48 carrier+compaction pass, 220 across affected suites, determinism 3/3. Fixes 2 + 4 still open.
- 2026-06-21 — **Implemented Fix 1** (the empty-floor PRIMARY fix), TDD Protocol A, uncommitted. Added pure `extract_user_constraints(views)` to `services/summarizer.py` (langchain-free L1 — harvests operator pins from human views) + wired it as the `user_constraints` arg to `derive_pinned_floor` at BOTH fold sites (`react_loop.py:2208` fold, `:1844` tail reinject), gated behind new default-OFF flag `context_extract_user_constraints` (env `CONTEXT_EXTRACT_USER_CONSTRAINTS`). RED→GREEN→VERIFY: 14 extractor L1 tests + 2 end-to-end carrier tests (flag-ON ⇒ must_not_count≥1 + non-empty hash; flag-OFF ⇒ reproduces the SHA-256-of-empty signature) + composition byte-identical/env-alias tests. 159 pass across the 4 affected suites; determinism 3/3; I-4/I-5 architecture guards green. Fixes 2–4 still open.
- 2026-06-21 — Ran the manual 20-turn fold probe (turns 1–12, stopped at the decisive Turn-12 §B2-R signal) on live rev `00097-wiz` (trigger=0.01) and recorded findings + a 4-fix plan in `c1_message_compaction.phase9.20turn_manual_probe.md`. **ROOT CAUSE:** both the fold floor (`react_loop.py:2208`) and tail reinjection (`:1844`) call `derive_pinned_floor(success_conditions, [])` with `user_constraints` hardcoded empty — the operator's conversational P1/P2/P3 pins are never extracted into the floor, so every fold carried an empty `constraint_floor_hash` (SHA-256 of `""`), `must_not_count=0`, `floor_reinjected=False` (hardcoded), `floor_exceeded=True`. P2 failed behaviorally at Turn 12 (model attempted `rm`, only the sandbox allowlist blocked it). Primary fix = a default-OFF pure user-constraint extractor feeding both call sites; plus carrier-honesty fixes for `floor_reinjected`/`tokens_after`-on-decline and an L2-shadow wiring check.
- 2026-06-21 — Added `c1_message_compaction.phases1-9_gcp_validation.walkthrough.md`: operator-runbook-voice teaching walkthrough validating the WHOLE C1 stack (Phases 1-9) on the live GCP `agent-backend-combined` service, driven by the 4-row synthetic `compaction` stress corpus. Per-step evidence triad (DOM screenshot + Langfuse `context.compacted`/`eval.compaction_fidelity` carriers + analyzer `--gate`); §B2-R inviolable bar (`unsafe_folds_total==0`) is the headline. Grounded in external research (ACON arXiv:2510.00615 long-horizon 26-54% token reduction; LongMemEval arXiv:2410.10813 evidence-in-haystack). Two prod-untouched checkpoints.
- 2026-06-21 — Added `c1_message_compaction.phase9.runbook.md`: operator-driven runbook for the Phase 9 live-validation step (tagged --no-traffic rev with the 8 CONTEXT_* env vars, `compaction`-phase planning-stress corpus run, analyzer gate). Also extended `scripts/build_planning_stress_corpus.py` with 4 `compaction`-phase rows and `scripts/analyze_planning_traces.py` with the `score_run` compaction branch + `gate_failures` bars (unsafe_folds_total == 0 INVIOLABLE; mean_drop_ratio ≥ 0.20 on the folded subset).
- 2026-06-21 — Added `c1_message_compaction.impl.md`: implementation companion to the C1 design doc (OKF Concept + machine-readable `todos:` array; the what-file/what-function/what-line/what-test build sheet). Design doc unchanged.
- 2026-06-20 — Declared `docs/plans/` an OKF bundle: added `index.md` + this `log.md` and typed frontmatter on every Concept (pure prepend, bodies unchanged). Convention in [CONVENTIONS_OKF.md](../CONVENTIONS_OKF.md); linted by `scripts/okf_lint.py`.
