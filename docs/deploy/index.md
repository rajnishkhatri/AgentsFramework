# Deployment walkthroughs — bundle index

OKF bundle. Each entry is a typed Concept. See the convention in [CONVENTIONS_OKF.md](../CONVENTIONS_OKF.md).

- [Deploy Piece C — memory recall/store ON (no-traffic) + thread-table migration](DEPLOY_PIECE_C.md) — Build the agent image from Dockerfile.backend, push it to Artifact Registry, and
- [Walkthrough — one authenticated memory run on the `mem` tag → trace fetch → governance audit](MEM_TAG_AUTHENTICATED_RUN.md) — /run/stream verifies a real WorkOS RS256 JWT against WorkOS's live JWKS
