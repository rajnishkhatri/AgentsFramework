# Thin wrapper — for reproducible/CI installs use requirements.lock directly:
#   pip install -r requirements.lock
#
# This file installs from the ranges in pyproject.toml (library-consumer mode).
# For exact reproducibility, prefer: pip install -r requirements.lock
-e ".[dev,gcp]"
