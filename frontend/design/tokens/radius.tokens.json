{
  "radius": {
    "sm": {
      "$value": "0.375rem",
      "$type": "dimension",
      "$description": "Small radius — buttons, inputs, chips"
    },
    "md": {
      "$value": "0.5rem",
      "$type": "dimension",
      "$description": "Medium radius — cards, panels, dialogs"
    },
    "lg": {
      "$value": "0.75rem",
      "$type": "dimension",
      "$description": "Large radius — pill composer, message cards, the soft Cursor-style chrome"
    }
  }
}
